// program printing out the size, min and max values of an int
fun main() {

    // prints the size
    println(Int.SIZE_BITS)

    // prints the min value
    println(Int.MIN_VALUE)

    // prints the max value
    println(Int.MAX_VALUE)
}